# Environment Variables for UniEngage Backend

# MongoDB Connection String
# For local development: mongodb://localhost:27017/uniengage
# For production (MongoDB Atlas): mongodb+srv://username:password@cluster.mongodb.net/uniengage
MONGO_URI=mongodb://localhost:27017/uniengage

# JWT Secret Key (use a secure random string in production)
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# Server Port (Render will set this automatically in production)
PORT=5000
